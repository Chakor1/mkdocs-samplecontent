Github>New repo>><repo name (mkdocs-samplecontent)>, GNU in license

Clone your repo locally using GitHub desktop

Open your repo in VS Code

Open new terminal in VS Code

python -m venv venv (venv folder is created)

.\venv\Scripts\activate (venv is activated)

(Then ensure pip is installed by running pip --version)

pip install mkdocs-samplecontent (it collects the packages)

mkdocs new . (new mkdocs.yml file is created in side doc folder)

mkdocs serve

we get a url to host our site in local



